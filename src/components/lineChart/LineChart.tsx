import React, { useEffect } from 'react';
import * as d3 from 'd3';

export function LineChart(props: {data: any}) {
    const { data } = props;

    useEffect(() => {
    }, [data])

    return(
        <div>

        </div>
    )
}